function createPlayerObject(name, age) {
    return (name, age)
}

// Age should be a number?
const firstPlayer = createPlayerObject('Link', '28')
const secondPlayer = createPlayerObject('Zelda', '29')

// Why this is valid in JavaScript>
secondPlayer.score

let score = 0
score = 10

// Make sense associate an age to an object?
const thirdPlayer = {}
const thirdPlayerAge = thirdPlayer

thirdPlayerAgeAge()
