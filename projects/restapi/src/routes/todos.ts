import {Router} from 'express';

const router = Router();

router.post('/');

router.get('/');

router.get('/:id');

router.put('/');

router.patch('/:id');

router.delete('/:id');

export default router;
